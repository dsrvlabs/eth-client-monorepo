# Placeholder so secrets/ exists in a clean checkout.
# Generate the Engine API JWT (32 bytes → 64 hex chars + newline):
#   openssl rand -hex 32 > secrets/jwt.hex && chmod 0600 secrets/jwt.hex
# Mounted read-only at /jwt/jwt.hex into el (and later engine). Never commit jwt.hex.
