[package]
name = "cc-serve-probe"
version.workspace = true
edition.workspace = true
rust-version.workspace = true
license.workspace = true
repository.workspace = true
publish.workspace = true

[[bin]]
name = "cc-serve-probe"
path = "src/main.rs"

[lints]
workspace = true

[dependencies]
# Allowed workspace edge ceiling when deps appear:
# {cc-libp2p, cc-types, cc-config} (Phase 4 store DAG). None required for the stub.
